- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_chache: yes
      changed_when: False

  tasks:
    - include_tasks:tasks/packages.yml
